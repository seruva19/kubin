import numpy as np
import torch
from typing import Optional, Tuple, Union
from diffusers.schedulers.scheduling_utils import SchedulerOutput


def create_weighted_embeds(pipeline, sampler, task="text2img"):
    pass
